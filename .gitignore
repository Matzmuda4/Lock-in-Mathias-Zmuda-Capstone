# ── Environment ───────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# ── Node / pnpm ───────────────────────────────────────────────────────────────
node_modules/
.pnpm-store/
dist/
.turbo/

# ── Tauri / Rust ──────────────────────────────────────────────────────────────
target/
apps/desktop/src-tauri/target/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Editor ────────────────────────────────────────────────────────────────────
.idea/
*.swp
*.swo

# ── Docker ────────────────────────────────────────────────────────────────────
# (volumes are named, no local bind-mounts to ignore)

# ── Uploaded files ────────────────────────────────────────────────────────────
services/api/uploads/

# ── Parsed PDF cache (extracted images + docling artifacts) ───────────────────
services/api/parsed_cache/

# ── Telemetry CSV exports ──────────────────────────────────────────────────────
services/api/exports/

# ── Test PDFs (local dev only) ────────────────────────────────────────────────
pdfs/

# ── Training artifacts ────────────────────────────────────────────────────────
training/checkpoints/
training/data/raw/
training/data/
training/exports/
training/Labelled/
training/adapters/
*.safetensors
*.bin
*.zip

# ── Master training data (generated, never committed) ─────────────────────────
# Keep README.md tracked; ignore all generated data files.
TrainingData/unlabelled.jsonl
TrainingData/baselines/
TrainingData/_append_index.json
TrainingData/.append.lock
TrainingData/labelled.jsonl

# ── Intermediate training datasets (labelling pipeline artefacts) ──────────────
# KEEP: intervention_train_v2.jsonl, intervention_eval_v2.jsonl,
#       intervention_training_v2_labelled.jsonl (final train/eval + base)
TrainingData/batches/
TrainingData/intervention_training_v2_skeletons.jsonl
TrainingData/intervention_training_v2_prelabelled.jsonl
TrainingData/intervention_training_raw.jsonl
TrainingData/intervention_training_labeled.jsonl
TrainingData/intervention_training_final.jsonl
TrainingData/supervised.jsonl
TrainingData/supervised_v2_backup.jsonl
TrainingData/intervention_eval.jsonl
TrainingData/intervention_train.jsonl
TrainingData/build_intervention_dataset.py
TrainingData/generate_synthetic.py
TrainingData/merge_supervised.py
TrainingData/eda.py

# ── LLM model weights and build artefacts (too large for git) ─────────────────
# adapter_model.safetensors is already covered by *.safetensors above
# tokenizer.json is large (~11 MB) but committed; only the binary weights are ignored
InterventionLLM/lockin_gguf/
InterventionLLM/lockin_merged/
InterventionLLM/llama.cpp-src/
InterventionLLM/Qwen2.5-7B-Instruct/

# ── Miscellaneous large/temp files ───────────────────────────────────────────
ziUIOguE

# ── Experiment results (raw participant data, never committed) ─────────────────
experimentresults/
lockin_P*_study.json
lockin_P*_adaptive_telemetry.csv
lockin_P*_baseline_telemetry.csv
